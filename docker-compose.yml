version: '3.8'

services:
  ragpilot:
    build: .
    container_name: ragpilot-app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
